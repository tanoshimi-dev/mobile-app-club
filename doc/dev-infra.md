Python, Node.js（React Native）development on Windows, Linux, MacOS.
Others ,database, mail , redis others all docker container.

## Component Structure

```
sys/
├── backend/            # URL: backend.mobile-app.club
|   ├── api/            # React Native mobile application (iOS / Android)
|   └── admin/          # React web application (Admin panel)
└── frontend/           # URL: mobile-app.club
    ├── mobile/         # React Native mobile application (iOS / Android user)
    └── web/            # React web application (Web user)
```
